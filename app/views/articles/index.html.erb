
<h1>Finance Updates</h1>

<table>
  <tr>
    <th>Date</th>
    <th>Real Estate</th>
    <th>Savings Accounts</th>
    <th>Checking Accounts</th>
    <th>Retirement Accounts</th>
    <th>Vehicles</th>
    <th>IRAs</th>
    <th>Stocks</th>

  </tr>

  <% @articles.each do |article| %>
    <tr>
      <td><%= article.date %></td>
      <td><%= article.real_estate %></td>
      <td><%= article.savings_accounts %></td>
      <td><%= article.checking_accounts%></td>
      <td><%= article.retirement_accounts%></td>
      <td><%= article.vehicles%></td>
      <td><%= article.iras%></td>
      <td><%= article.stocks%></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Delete', article_path(article),
               method: :delete,
               data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<br>
<%= link_to 'Back', welcome_index_path %>
